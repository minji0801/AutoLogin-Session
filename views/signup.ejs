<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원가입</title>

    <!-- Google Font -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Jua&display=swap" rel="stylesheet">

    <!-- css -->
    <link rel="stylesheet" href="/stylesheets/autologin_signup.css" type="text/css">

    <!-- js -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

    <style>
        /* * {
            font-family: 'Jua', sans-serif;
        } */
    </style>
    <script>
        function signup() {
            var name = $('#name').val();
            var email = $('#email').val();
            var pw = $('#pw').val();

            $.ajax({
                type: 'POST',
                url: '/users/signup',
                data: { name: name, email: email, pw: pw },
                dataType: "json",
                success: function (response) {

                    console.log(response.data);

                    if (response.data == 'NO') {

                        alert('이미 가입한 사용자입니다.');

                    } else if (response.data == 'OK') {

                        // 메인으로 이동
                        location.href = '/welcome';
                    }
                }
            });
        }

        // 창 종료 이벤트
        $(window).bind("beforeunload", function () {

            $.ajax({
                type: 'POST',
                url: '/users/close',
                success: function (response) {

                }
            });
        })
    </script>
</head>

<body class="container">
    <div class="wrap">
        <div class="title">회원가입</div>
        <div class="desc">이메일로 계정을 만들 수 있어요</div>
        <div>
            <input class="input-name" type="text" id="name" placeholder="이름" />
            <input class="input-email" type="text" id="email" placeholder="이메일" />
            <input class="input-pw" type="password" id="pw" placeholder="비밀번호" />
        </div>
        <div>
            <button class="btn signup" onclick="signup()">가입하기</button>
        </div>
    </div>
</body>

</html>